# Core dependencies
numpy
opencv-python
pillow
pyyaml
python-dotenv
smbus2
RPi.GPIO; sys_platform == "linux"
adafruit-circuitpython-mpu6050; sys_platform == "linux"
flask
flask-socketio
geopy
imutils
networkx
pathfinding
pyserial
rtree
shapely
# tensorflow # Commented out: Full TF likely not needed on Pi, tflite-runtime is used.
colorama
watchdog

# YOLOv8 dependencies
ultralytics
onnx
onnxruntime

# System monitoring and safety
psutil
py3nvml
pyudev; sys_platform == "linux"
systemd-python
python-daemon
supervisor
prometheus_client

# GPS and navigation
pynmea2
gpsd-py3; sys_platform == "linux"
utm

# Sensors and hardware
adafruit-circuitpython-bme280; sys_platform == "linux"
adafruit-circuitpython-bno08x; sys_platform == "linux"
adafruit-circuitpython-ina3221; sys_platform == "linux"
adafruit-circuitpython-vl53l0x; sys_platform == "linux"
# picamera2 is installed via apt (python3-picamera2)

# Coral TPU dependencies
# Ensure tflite-runtime is present for Raspberry Pi (Linux)
tflite-runtime
# Ensure pycoral is present for Coral support on Raspberry Pi (Linux)

# Security
safety
bandit
pyOpenSSL
certifi

# Monitoring
grafana-client; sys_platform == "linux"
statsd

# Development dependencies
pytest
black
flake8
mypy
pytest-cov
pytest-mock
pytest-asyncio
pytest-timeout
pytest-xdist
coverage
tox
sphinx
sphinx-rtd-theme
pre-commit

flask-cors
scipy
pydantic
Flask-Babel
